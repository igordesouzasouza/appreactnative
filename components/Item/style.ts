import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        paddingTop: 24,
        flexDirection: "row",
        alignItems: "center",
        gap: 7
    },
    description: {
        flex: 1,
        fontSize: 14,
        fontWeight: 600
    },
})